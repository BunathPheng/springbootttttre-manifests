---
# Source: helm for kubernetes/templates/ingress.yaml
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: springbootttttre                  # Ingress name
  labels:
    app: springbootttttre
spec:
  ingressClassName: nginx   # Optional ingressClass
  rules:
    - host: "springbootttttre.smartops.space"
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: springbootttttre               # Links to Service name dynamically
                port:
                  number: 8080  # Links to service port dynamically
